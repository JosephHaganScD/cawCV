% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/partition_naive_kfold.R
\name{partition_naive_kfold}
\alias{partition_naive_kfold}
\title{Naive observation-level k-fold partition}
\usage{
partition_naive_kfold(cluster, k = 10, seed = NULL)
}
\arguments{
\item{cluster}{A vector of subject or cluster identifiers, one element per
observation. Only its length is used. The grouping is intentionally
ignored, which is what makes this strategy leakage-prone.}

\item{k}{Number of folds. A single integer of at least 2. Defaults to 10.}

\item{seed}{Optional integer. If supplied, the global random number
generator state is set with \code{\link[=set.seed]{set.seed()}} before fold assignment so the
partition is reproducible.}
}
\value{
A \code{caw_partition} object.
}
\description{
Assigns each observation to one of \code{k} folds at random, ignoring subject
clustering. Observations from the same subject may therefore be split across
the training and test sets. This is the leakage-prone baseline strategy and
is provided for comparison with the cluster-aware strategies.
}
\examples{
subject <- rep(1:6, each = 5)
p <- partition_naive_kfold(subject, k = 5, seed = 1)
p
get_fold(p, 1)
}
\seealso{
\code{\link[=partition_subject_kfold]{partition_subject_kfold()}}, \code{\link[=partition_loco]{partition_loco()}}
}
