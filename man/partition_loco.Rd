% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/partition_loco.R
\name{partition_loco}
\alias{partition_loco}
\title{Leave-one-cluster-out partition}
\usage{
partition_loco(cluster)
}
\arguments{
\item{cluster}{A vector of subject or cluster identifiers, one element per
observation.}
}
\value{
A \code{caw_partition} object with as many folds as there are distinct
clusters.
}
\description{
Creates one fold per subject (cluster). Each fold holds out all observations
from a single subject, and the remaining subjects form the training set.
This is the exhaustive cluster-aware strategy. It is deterministic, so no
random seed is required.
}
\examples{
subject <- rep(1:8, each = 4)
p <- partition_loco(subject)
p
n_folds(p)
}
\seealso{
\code{\link[=partition_naive_kfold]{partition_naive_kfold()}}, \code{\link[=partition_subject_kfold]{partition_subject_kfold()}}
}
