% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predictor_icc.R
\name{predictor_icc}
\alias{predictor_icc}
\title{Per-predictor intraclass correlation}
\usage{
predictor_icc(data, predictors, cluster)
}
\arguments{
\item{data}{A data frame containing the predictor columns.}

\item{predictors}{A character vector of column names in \code{data} for which the
ICC is required.}

\item{cluster}{A vector of subject or cluster identifiers, of length equal
to \code{nrow(data)}.}
}
\value{
A data frame with one row per predictor and two columns,
\code{predictor} and \code{icc}. The ICC can be slightly negative when
between-subject variance is negligible, and is \code{NA} when it cannot be
estimated.
}
\description{
Computes the intraclass correlation (ICC) of each predictor across subjects,
using a one-way random-effects analysis of variance with an adjustment for
unbalanced cluster sizes. The ICC quantifies the share of a predictor's
total variance that lies between subjects rather than within them.
Predictors with a high ICC carry strong within-subject dependence and are
the predictors through which observation-level cross-validation leaks
information.
}
\examples{
set.seed(1)
n_subj <- 20
n_obs <- 6
subject <- rep(seq_len(n_subj), each = n_obs)
b <- rnorm(n_subj)[subject]
dat <- data.frame(
  subject = subject,
  x1 = b + rnorm(n_subj * n_obs),   # carries subject-level structure
  x2 = rnorm(n_subj * n_obs)        # no subject-level structure
)
predictor_icc(dat, c("x1", "x2"), dat$subject)
}
\seealso{
\code{\link[=predictor_lag1]{predictor_lag1()}}
}
