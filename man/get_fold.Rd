% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/caw_partition.R
\name{get_fold}
\alias{get_fold}
\title{Extract the training and test rows for one fold}
\usage{
get_fold(partition, fold)
}
\arguments{
\item{partition}{A \code{caw_partition} object.}

\item{fold}{A single integer identifying the fold, between 1 and
\code{n_folds(partition)}.}
}
\value{
A list with two integer vectors, \code{train} and \code{test}, giving the row
positions assigned to the training and test sets for the requested fold.
}
\description{
Extract the training and test rows for one fold
}
\examples{
p <- partition_subject_kfold(rep(1:12, each = 5), k = 4, seed = 1)
fold1 <- get_fold(p, 1)
str(fold1)
}
