% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predictor_lag1.R
\name{predictor_lag1}
\alias{predictor_lag1}
\title{Per-predictor within-subject lag-1 autocorrelation}
\usage{
predictor_lag1(data, predictors, cluster, time = NULL)
}
\arguments{
\item{data}{A data frame containing the predictor columns.}

\item{predictors}{A character vector of column names in \code{data} for which the
lag-1 autocorrelation is required.}

\item{cluster}{A vector of subject or cluster identifiers, of length equal
to \code{nrow(data)}.}

\item{time}{Optional vector of length equal to \code{nrow(data)} giving the
measurement time within subject. If supplied, observations are ordered by
\code{time} within each subject before the autocorrelation is computed. If
\code{NULL}, the existing row order within each subject is assumed to be time
order.}
}
\value{
A data frame with one row per predictor and four columns:
\code{predictor}, \code{lag1_median}, \code{lag1_q25} and \code{lag1_q75}. Subjects with fewer
than three non-missing observations contribute \code{NA} and are excluded from
the summary.
}
\description{
Computes, for each predictor, the lag-1 autocorrelation within each subject
and then summarises those values across subjects by their median and
interquartile range. Lag-1 autocorrelation measures the temporal
predictability of a predictor within a subject, the mechanism through which
observation-level cross-validation leaks information directly between
consecutive records of the same subject.
}
\examples{
set.seed(1)
n_subj <- 20
n_obs <- 30
subject <- rep(seq_len(n_subj), each = n_obs)
# x1 is an autocorrelated within-subject series; x2 is white noise
x1 <- as.vector(sapply(seq_len(n_subj), function(i) {
  as.numeric(arima.sim(list(ar = 0.7), n = n_obs))
}))
x2 <- rnorm(n_subj * n_obs)
dat <- data.frame(subject = subject, x1 = x1, x2 = x2)
predictor_lag1(dat, c("x1", "x2"), dat$subject)
}
\seealso{
\code{\link[=predictor_icc]{predictor_icc()}}
}
